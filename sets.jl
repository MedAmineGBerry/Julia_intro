# Use sets to represent collections of unordered, unique values.
a = Set()
a = Set([1,2,2,3,4]) # 2 will appear only once
intersect(set1,set2) #intersection
union(set1,set2) # union
setdiff(set1,set2) # differences